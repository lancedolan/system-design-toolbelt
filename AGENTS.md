# AGENTS.md

- This is a single-page app hosted on GitHub Pages, so it aims for zero dependencies.
- The goal is a simple UI where engineers can either learn system design patterns or quiz themselves on when to use them.
- Patterns are framed as "tools" in a "toolbelt" that engineers reach for when they recognize certain triggers.
